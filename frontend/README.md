# frontend — Frontend (Next.js + TypeScript)

หน้าเว็บของ MindKeep (App Router). โครง: `app/` = routes จริง · `features/` = โมดูลฟีเจอร์ตาม domain ·
`components/` = UI กลางใช้ซ้ำ · `lib/api/` = typed client ที่เดียวที่คุย backend · `hooks/` `types/` `styles/` `public/`.
ดู [PROJECT_MAP.md](../docs/PROJECT_MAP.md).
