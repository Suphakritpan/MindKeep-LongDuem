# features/ — feature-first modules

แต่ละ subfolder = หนึ่ง domain (ให้ตรงกับ backend module เช่น documents, memory, chat) รวม
`components/ · hooks/ · api.ts · types.ts` ของ feature นั้นไว้ที่เดียว. `documents/` เป็นตัวอย่าง pattern.
