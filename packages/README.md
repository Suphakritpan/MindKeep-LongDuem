# packages/ — shared code (OPTIONAL)

สำหรับโค้ดที่ web กับ api ใช้ร่วมกันจริง (เช่น shared types/ui/config). **ยังว่างโดยตั้งใจ** —
เริ่มในแต่ละ app ก่อน แล้วค่อยยกขึ้นมาเมื่อมีการแชร์ซ้ำจริง (กัน over-engineering).
