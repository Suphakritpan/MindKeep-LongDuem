# MindKeep-LongDuem
The project is a real, working on-premise AI knowledge workspace for a Thai SME (demo tenant LongDuem), with a genuinely functional Finance MVP. The **code is in better shape than the documentation**: the backend is coherent and the Finance + Auth + OCR + AI-search paths are implemented and tested.
